# Supabase Configuration
# For cloud mode, create a Supabase project at https://supabase.com
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-anon-key-here
VITE_SUPABASE_PROJECT_ID=your-project-id

# Provider Switch
# Choose database provider for the frontend: 'supabase' (default) or 'rest'
VITE_DB_PROVIDER=supabase

# REST API base URL (used when VITE_DB_PROVIDER=rest)
VITE_API_BASE_URL=http://localhost:8787

# Auth providers to enable in UI (comma-separated): 'supabase', 'local'
VITE_AUTH_PROVIDERS=supabase

# Authentication Configuration
# Email domain restriction for user authentication
# Set to the domain you want to allow (e.g., @yourcompany.com)
# Users with emails from other domains will be denied access
VITE_ALLOWED_EMAIL_DOMAIN=@example.com

# Development-only test account bypass for domain validation
# Set to 'true' to allow admin@test.local and member@test.local in development
VITE_ALLOW_DEV_TEST_ACCOUNTS=false

# Google Workspace Configuration (Optional)
# If using Google OAuth with a specific workspace, set the domain here
# This adds a domain hint to the Google sign-in flow
# Leave empty to allow any Google account
VITE_GOOGLE_WORKSPACE_DOMAIN=

# Application Branding (Optional)
# Customize the application name and description
VITE_APP_NAME=Quorum
VITE_APP_DESCRIPTION=Organize and manage your SQL queries securely

# Docker Compose / Backend (REST provider)
# Set these when running docker-compose for Postgres-only or Dual modes
# POSTGRES_PASSWORD=
# POSTGRES_DB=
# DATABASE_URL=
# Example: DATABASE_URL=postgres://postgres:<your-password>@localhost:5432/<your-db>
